# Preserved pre-Rust generation

The pre-Rust repositories are intentionally not copied into this source tree.
Migration behavior is recorded in `docs/MIGRATION.md` without publishing local
machine paths or duplicating storage-heavy working copies.
